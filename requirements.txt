# Core pipeline (sir_cs_pipeline_optimized.py)
numpy>=1.20
pandas>=1.3
matplotlib>=3.4
scikit-learn>=1.0

# Etapa 1 roadmap: SPGL1 via PyLops (profile solver_comparison)
pylops>=2.0
spgl1>=2.0

# Etapa 2 LFISTA: sir_cs_lfista.py and pipeline profiles lfista_integrated* (skip if you only run classic profiles)
torch>=2.0

# Extra orthonormal bases for residual sparsification (haar, db4, sym4) in real-well benchmark
PyWavelets>=1.4
